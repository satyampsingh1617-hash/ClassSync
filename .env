# Always point to Railway backend — works for both local dev and Android builds.
# The Vite dev proxy is NOT used because VITE_API_URL is set.
# If you want to test against a local backend, comment this out and run: node backend/server.js
VITE_API_URL=https://attendpro-backend-production-e76e.up.railway.app/api
